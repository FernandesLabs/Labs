# Changelog

All notable changes to the Fernandes Labs tool network are documented here.
Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

### Added
- JSON ↔ YAML Converter (developer) — Convert between JSON and YAML formats. (`/tools/developer/json-yaml-converter/`)
- XML Formatter (developer) — Format and pretty-print XML. (`/tools/developer/xml-formatter/`)
- Base64 Encoder / Decoder (developer) — Encode text to Base64 or decode Base64 to text. (`/tools/developer/base64-encoder-decoder/`)
- JWT Decoder (developer) — Decode and inspect JSON Web Token header and payload. (`/tools/developer/jwt-decoder/`)
- JWT Generator (developer) — Generate JSON Web Tokens with custom header and payload. (`/tools/developer/jwt-generator/`)
- Regex Tester (developer) — Test regular expressions against text with match highlighting. (`/tools/developer/regex-tester/`)
- Cron Expression Generator (developer) — Build cron expressions with a visual interface. (`/tools/developer/cron-expression-generator/`)
- Unix Timestamp Converter (developer) — Convert between Unix timestamps and human-readable dates. (`/tools/developer/unix-timestamp-converter/`)
- Hash Generator (developer) — Generate MD5, SHA-1, SHA-256, and SHA-512 hashes. (`/tools/developer/hash-generator/`)
- HMAC Generator (developer) — Generate HMAC signatures with SHA-256. (`/tools/developer/hmac-generator/`)
- URL Encoder / Decoder (developer) — Encode or decode URL components. (`/tools/developer/url-encoder-decoder/`)
- URL Parser (developer) — Break down a URL into its components. (`/tools/developer/url-parser/`)
- Query String Builder (developer) — Build URL query strings from key-value pairs. (`/tools/developer/query-string-builder/`)
- HTML Entity Encoder (developer) — Encode or decode HTML entities. (`/tools/developer/html-entity-encoder/`)
- Color Converter (developer) — Convert between HEX, RGB, HSL, and OKLCH color formats. (`/tools/developer/color-converter/`)
- CSS Gradient Generator (developer) — Create CSS linear gradients visually. (`/tools/developer/css-gradient-generator/`)
- CSS Box Shadow Generator (developer) — Generate CSS box-shadow properties. (`/tools/developer/css-box-shadow-generator/`)
- CSS Border Radius Generator (developer) — Generate CSS border-radius with visual sliders. (`/tools/developer/css-border-radius-generator/`)
- SQL Formatter (developer) — Format and pretty-print SQL queries. (`/tools/developer/sql-formatter/`)
- SQL Pretty Printer (developer) — Beautify SQL with proper indentation. (`/tools/developer/sql-pretty-printer/`)
- Markdown Preview (developer) — Preview Markdown as you type. (`/tools/developer/markdown-preview/`)
- Markdown → HTML (developer) — Convert Markdown to standalone HTML. (`/tools/developer/markdown-to-html/`)
- HTML Minifier (developer) — Minify HTML by removing whitespace and comments. (`/tools/developer/html-minifier/`)
- CSS Minifier (developer) — Minify CSS by removing whitespace and comments. (`/tools/developer/css-minifier/`)
- JS Minifier (developer) — Basic JavaScript minifier — removes comments and whitespace. (`/tools/developer/js-minifier/`)
- Diff Checker (developer) — Compare two text blocks and show differences. (`/tools/developer/diff-checker/`)
- Text Compare (developer) — Compare two texts line by line and highlight changes. (`/tools/developer/text-compare/`)
- Barcode Generator (developer) — Generate barcodes from text or numbers. (`/tools/developer/barcode-generator/`)
- API Request Builder (developer) — Build HTTP API requests with headers and body. (`/tools/developer/api-request-builder/`)
- Character Counter (text) — Count characters, words, and paragraphs. (`/tools/text/character-counter/`)
- Reading Time Calculator (text) — Estimate reading time for any text. (`/tools/text/reading-time-calculator/`)
- Case Converter (text) — Convert text to UPPER, lower, Title, camelCase, snake_case, kebab-case. (`/tools/text/case-converter/`)
- Slug Generator (text) — Generate URL-safe slugs from any text. (`/tools/text/slug-generator/`)
- Lorem Ipsum Generator (text) — Generate Lorem Ipsum placeholder text. (`/tools/text/lorem-ipsum-generator/`)
- Duplicate Line Remover (text) — Remove duplicate lines from text. (`/tools/text/duplicate-line-remover/`)
- Text Sorter (text) — Sort lines alphabetically, numerically, or reverse. (`/tools/text/text-sorter/`)
- Remove Blank Lines (text) — Strip empty lines from text. (`/tools/text/remove-blank-lines/`)
- Remove Duplicate Words (text) — Remove duplicate words from text. (`/tools/text/remove-duplicate-words/`)
- Capitalization Tool (text) — Capitalize sentences, words, or first letters. (`/tools/text/capitalization-tool/`)
- Unicode Inspector (text) — Inspect the Unicode code points of any text. (`/tools/text/unicode-inspector/`)
- Robots.txt Generator (seo) — Generate a robots.txt file for your site. (`/tools/seo/robots-txt-generator/`)
- Sitemap Generator (seo) — Generate an XML sitemap from a list of URLs. (`/tools/seo/sitemap-generator/`)
- Open Graph Preview (seo) — Preview how your page looks when shared on social media. (`/tools/seo/open-graph-preview/`)
- Twitter Card Preview (seo) — Preview how your page appears in Twitter cards. (`/tools/seo/twitter-card-preview/`)
- Canonical URL Checker (seo) — Check and set canonical URLs for pages. (`/tools/seo/canonical-url-checker/`)
- Keyword Density Checker (seo) — Analyze keyword density in text. (`/tools/seo/keyword-density-checker/`)
- Heading Structure Analyzer (seo) — Analyze heading hierarchy (H1–H6) in HTML. (`/tools/seo/heading-structure-analyzer/`)
- JSON-LD Generator (seo) — Generate JSON-LD structured data for SEO. (`/tools/seo/json-ld-generator/`)
- FAQ Schema Generator (seo) — Generate FAQPage JSON-LD structured data. (`/tools/seo/faq-schema-generator/`)
- Breadcrumb Schema Generator (seo) — Generate BreadcrumbList JSON-LD schema. (`/tools/seo/breadcrumb-schema-generator/`)
- Organization Schema Generator (seo) — Generate Organization JSON-LD structured data. (`/tools/seo/organization-schema-generator/`)
- VAT Calculator (finance) — Add or remove VAT from a price. (`/tools/finance/vat-calculator/`)
- Profit Margin Calculator (finance) — Calculate profit margin from cost and revenue. (`/tools/finance/profit-margin-calculator/`)
- ROI Calculator (finance) — Calculate return on investment. (`/tools/finance/roi-calculator/`)
- Break-even Calculator (finance) — Find the break-even point for your business. (`/tools/finance/break-even-calculator/`)
- Invoice Number Generator (finance) — Generate sequential invoice numbers with a prefix. (`/tools/finance/invoice-number-generator/`)
- Quote Generator (finance) — Generate a simple price quote. (`/tools/finance/quote-generator/`)
- Currency Converter (finance) — Convert between currencies using static rates. (`/tools/finance/currency-converter/`)
- Unit Converter (finance) — Convert length, weight, and temperature units. (`/tools/finance/unit-converter/`)
- Compound Interest Calculator (finance) — Calculate compound interest over time. (`/tools/finance/compound-interest-calculator/`)
- Loan Calculator (finance) — Calculate monthly loan payments. (`/tools/finance/loan-calculator/`)
- Mortgage Calculator (finance) — Calculate monthly mortgage payments. (`/tools/finance/mortgage-calculator/`)
- Savings Calculator (finance) — Project savings growth with regular deposits. (`/tools/finance/savings-calculator/`)
- FIRE Calculator (finance) — Calculate Financial Independence / Retire Early number. (`/tools/finance/fire-calculator/`)
- Inflation Calculator (finance) — Calculate the future value of money with inflation. (`/tools/finance/inflation-calculator/`)
- Retirement Calculator (finance) — Project retirement savings and income. (`/tools/finance/retirement-calculator/`)
- Password Strength Checker (security) — Check password strength with entropy analysis. (`/tools/security/password-strength-checker/`)
- Secure Passphrase Generator (security) — Generate diceware-style passphrases with Web Crypto. (`/tools/security/secure-passphrase-generator/`)
- CSP Generator (security) — Generate Content-Security-Policy headers. (`/tools/security/csp-generator/`)
- CORS Header Tester (security) — Check CORS headers on any URL. (`/tools/security/cors-header-tester/`)
- Security Header Checker (security) — Check security headers on any URL. (`/tools/security/security-header-checker/`)
- Image Compressor (misc) — Compress images in-browser using canvas. (`/tools/misc/image-compressor/`)
- Image Resizer (misc) — Resize images to custom dimensions. (`/tools/misc/image-resizer/`)
- SVG Optimizer (misc) — Minify SVG by removing comments and metadata. (`/tools/misc/svg-optimizer/`)
- SVG Viewer (misc) — Paste SVG code and preview it live. (`/tools/misc/svg-viewer/`)
- SVG → PNG (misc) — Convert SVG to PNG in your browser. (`/tools/misc/svg-to-png/`)
- PNG → WebP (misc) — Convert PNG images to WebP format. (`/tools/misc/png-to-webp/`)
- Image Metadata Viewer (misc) — View EXIF and basic image metadata. (`/tools/misc/image-metadata-viewer/`)
- Color Palette Extractor (misc) — Extract dominant colors from an image. (`/tools/misc/color-palette-extractor/`)
- Favicon Generator (misc) — Generate favicon PNGs from an image. (`/tools/misc/favicon-generator/`)
- Social Image Generator (misc) — Generate 1200×630 social preview images. (`/tools/misc/social-image-generator/`)
- Merge PDFs (misc) — Merge multiple PDF files into one (client-side). (`/tools/misc/merge-pdfs/`)
- Split PDF (misc) — Split a PDF into individual pages. (`/tools/misc/split-pdf/`)
- Compress PDF (misc) — Reduce PDF file size. (`/tools/misc/compress-pdf/`)
- PDF → Images (misc) — Convert PDF pages to images. (`/tools/misc/pdf-to-images/`)
- Images → PDF (misc) — Combine images into a PDF document. (`/tools/misc/images-to-pdf/`)
- Rotate PDF (misc) — Rotate pages in a PDF document. (`/tools/misc/rotate-pdf/`)
- Extract PDF Pages (misc) — Extract specific pages from a PDF. (`/tools/misc/extract-pdf-pages/`)
- PDF Metadata Viewer (misc) — View PDF metadata and properties. (`/tools/misc/pdf-metadata-viewer/`)
- Prompt Optimizer (misc) — Improve AI prompts with structure and clarity. (`/tools/misc/prompt-optimizer/`)
- Prompt Library (misc) — Browse and copy reusable AI prompt templates. (`/tools/misc/prompt-library/`)
- System Prompt Generator (misc) — Build structured system prompts for AI assistants. (`/tools/misc/system-prompt-generator/`)
- AI Persona Generator (misc) — Create detailed personas for AI role-play. (`/tools/misc/ai-persona-generator/`)
- Prompt Version Manager (misc) — Track and compare prompt versions. (`/tools/misc/prompt-version-manager/`)
- Prompt Variable Replacer (misc) — Replace {{variables}} in prompt templates. (`/tools/misc/prompt-variable-replacer/`)
- Token Counter (misc) — Estimate token count for AI prompts. (`/tools/misc/token-counter/`)
- AI Cost Calculator (misc) — Estimate AI API costs based on token usage. (`/tools/misc/ai-cost-calculator/`)
- AI Workflow Builder (misc) — Design multi-step AI workflows. (`/tools/misc/ai-workflow-builder/`)
- UTM Builder (seo) — Build UTM tracking URLs for campaigns. (`/tools/seo/utm-builder/`)
- Campaign URL Builder (seo) — Build campaign URLs with full UTM parameters. (`/tools/seo/campaign-url-builder/`)
- Email Signature Generator (seo) — Generate professional email signatures. (`/tools/seo/email-signature-generator/`)
- Invoice Generator (finance) — Generate simple invoices. (`/tools/finance/invoice-generator/`)
- QR Campaign Generator (seo) — Generate QR codes for marketing campaigns. (`/tools/seo/qr-campaign-generator/`)
- Landing Page Audit (seo) — Audit a landing page for key elements. (`/tools/seo/landing-page-audit/`)
- CTA Generator (seo) — Generate call-to-action button text. (`/tools/seo/cta-generator/`)
- Title Generator (seo) — Generate SEO-optimized page titles. (`/tools/seo/title-generator/`)
- Meta Description Generator (seo) — Generate SEO meta descriptions. (`/tools/seo/meta-description-generator/`)
- FAQ Generator (seo) — Generate FAQ content from a topic. (`/tools/seo/faq-generator/`)
- Blog Outline Generator (seo) — Generate blog post outlines from a topic. (`/tools/seo/blog-outline-generator/`)
- Headline Analyzer (seo) — Score headlines for engagement and SEO. (`/tools/seo/headline-analyzer/`)
- BMI Calculator (finance) — Calculate Body Mass Index. (`/tools/finance/bmi-calculator/`)
- BMR Calculator (finance) — Calculate Basal Metabolic Rate. (`/tools/finance/bmr-calculator/`)
- TDEE Calculator (finance) — Calculate Total Daily Energy Expenditure. (`/tools/finance/tdee-calculator/`)
- Body Fat Calculator (finance) — Estimate body fat percentage. (`/tools/finance/body-fat-calculator/`)
- Water Intake Calculator (finance) — Calculate daily water intake needs. (`/tools/finance/water-intake-calculator/`)
- Macro Calculator (finance) — Calculate macronutrient needs. (`/tools/finance/macro-calculator/`)
- Calorie Calculator (finance) — Calculate daily calorie needs. (`/tools/finance/calorie-calculator/`)
- GPA Calculator (finance) — Calculate Grade Point Average. (`/tools/finance/gpa-calculator/`)
- Grade Calculator (finance) — Calculate weighted grades. (`/tools/finance/grade-calculator/`)
- Citation Generator (finance) — Generate APA, MLA, and Chicago citations. (`/tools/finance/citation-generator/`)
- Flashcard Generator (finance) — Create and study digital flashcards. (`/tools/finance/flashcard-generator/`)
- Study Planner (finance) — Plan study sessions and track progress. (`/tools/finance/study-planner/`)
- DNS Lookup (misc) — Look up DNS records for a domain. (`/tools/misc/dns-lookup/`)
- WHOIS Lookup (misc) — Look up domain registration info. (`/tools/misc/whois-lookup/`)
- IP Lookup (misc) — Look up IP address information. (`/tools/misc/ip-lookup/`)
- SSL Checker (misc) — Check SSL certificate for a domain. (`/tools/misc/ssl-checker/`)
- HTTP Header Checker (misc) — Check HTTP response headers. (`/tools/misc/http-header-checker/`)
- Redirect Checker (misc) — Check redirect chain for a URL. (`/tools/misc/redirect-checker/`)
- Ping Tool (misc) — Check if a website is reachable. (`/tools/misc/ping-tool/`)
- User-Agent Parser (misc) — Parse a User-Agent string. (`/tools/misc/user-agent-parser/`)
- ZIP Extractor (misc) — List and extract files from ZIP archives. (`/tools/misc/zip-extractor/`)
- TAR Extractor (misc) — List and extract files from TAR archives. (`/tools/misc/tar-extractor/`)
- File Checksum (misc) — Generate checksums for files. (`/tools/misc/file-checksum/`)
- File Hash (misc) — Generate multiple hashes for a file. (`/tools/misc/file-hash/`)
- MIME Detector (misc) — Detect MIME type from file signature. (`/tools/misc/mime-detector/`)
- File Size Converter (misc) — Convert between bytes, KB, MB, GB. (`/tools/misc/file-size-converter/`)
- File Signature Inspector (misc) — Inspect file magic bytes / signatures. (`/tools/misc/file-signature-inspector/`)
- Color Contrast Checker (misc) — Check WCAG color contrast ratios. (`/tools/misc/color-contrast-checker/`)
- Alt Text Generator (misc) — Generate accessibility alt text for images. (`/tools/misc/alt-text-generator/`)
- ARIA Validator (misc) — Validate ARIA attributes in HTML. (`/tools/misc/aria-validator/`)
- Font Accessibility Checker (misc) — Check font sizes for accessibility compliance. (`/tools/misc/font-accessibility-checker/`)

- Automation System — `create-tool.sh`, `qa-check.sh`, `build-all.sh`, `deploy.sh`, and GitHub Actions CI/CD workflow. (`/automation/`, `/.github/workflows/deploy.yml`)
- UUID Generator (developer) — scaffolded by `create-tool.sh` as a test; implements RFC 4122 UUID generation via Web Crypto API. (`/tools/developer/uuid-generator/`)
- QR Generator (misc) — QR codes from text/URL, PNG download. (`/tools/misc/qr-generator/`)
- Percentage Calculator (finance) — three percentage calculation modes. (`/tools/finance/percentage-calculator/`)
- Word Counter (text) — live word/char/sentence count + reading time. (`/tools/text/word-counter/`)
- Meta Tag Generator (seo) — SEO meta tag generation (title, OG, Twitter, canonical). (`/tools/seo/meta-tag-generator/`)
- JSON Formatter (developer) — format, minify, validate JSON. (`/tools/developer/json-formatter/`)
- Test Tool (developer) — A test tool for validation (`/tools/developer/test-tool/`)
- UUID Generator (developer) — Generate RFC 4122 UUIDs with Web Crypto API (`/tools/developer/uuid-generator/`)

---

## [0.1.0] — 2026-07-17

### Added
- Central configuration file (`config.json`) controlling AdSense, crypto wallets, analytics, branding, and SEO defaults across all tools.
- Shared config loader (`assets/config-loader.js`) — fetches `/config.json`, caches in localStorage (1h TTL), deep-merges with fallback defaults so tools work even if the fetch fails.
- Shared design system (`assets/styles.css`) — semantic CSS tokens (light/dark via `[data-theme]`), spacing scale, typography, component classes (buttons, cards, inputs, header, footer, ad placeholders).
- Tools index (`index.html`) — searchable grid with category badges, loads config.
- Fernandes Labs logo (`assets/logo.svg`).
- Restrictive license (`LICENSE`) — Fernandes Labs Source License (FLSL) v1.0. Commercial Use (ads, affiliate links, donations, paid products) requires a paid Commercial License. Non-Commercial Use permitted with attribution.
- Progress ledger (`PROGRESS.md`) — permanent audit trail of every tool built.
- README with setup, deployment (GitHub Pages / Cloudflare Pages), AdSense approval checklist, and licensing terms.

### Decisions
- Chose standalone single-file HTML tools (not a framework) so each tool is independently deployable and has zero build step.
- Chose a central `config.json` over per-tool config so monetization settings update network-wide in one edit.
- Chose the FLSL license over MIT to create a legal moat — others cannot legally monetize this code without a paid Commercial License.
